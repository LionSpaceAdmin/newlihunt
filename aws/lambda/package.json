{
  "name": "scam-hunt-lambda-functions",
  "version": "1.0.0",
  "description": "Lambda functions for Scam Hunt Platform",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "package": "npm run package:analyze && npm run package:url-inspector",
    "package:analyze": "cd analyze && zip -r ../packages/analyze.zip . -x node_modules/.cache/*",
    "package:url-inspector": "cd url-inspector && zip -r ../packages/url-inspector.zip . -x node_modules/.cache/*"
  },
  "dependencies": {
    "aws-sdk": "^2.1490.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.54.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Scam Hunt Team",
  "license": "MIT"
}